@model ControleDeCinema.WebApp.Models.NotificacaoViewModel
@{
    Layout = "_Layout";
    ViewBag.Title = "Notificação";
    ViewBag.Title = "Mensagem";
}

<div>
    <h3>@Model.Mensagem</h3>
    
    <a href="@Model.LinkRedirecionamento" class="btn btn-secondary px-5 mt-5">
        <i class="bi bi-arrow-return-left me-1"></i>
        Voltar
    </a>
<div class="container">
    <div class="row p-3 ">
        <h3 class="text-center mt-3">@ViewBag.Mensagem</h3>
    </div>
</div>