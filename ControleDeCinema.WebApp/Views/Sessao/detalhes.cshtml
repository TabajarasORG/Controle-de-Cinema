@model ControleDeCinema.WebApp.Models.DetalhesSessaoViewModel

@{
    Layout = "_Layout";
    ViewBag.Title = "Visualizar Detalhes da Sessao";
}

<div class="row mt-3">
    <div class="col-lg-6 d-grid gap-2 d-md-block">

        <div class="d-grid">
            <label class="fs-5"><strong>Sessão:</strong> @Model.Filme</label>

            <label><strong>Id:</strong> [@Model.Id]</label>
            <label><strong>Filme:</strong> @Model.Filme.ToString()</label>
            <label><strong>Sala:</strong> @Model.Sala.ToString()</label>
            <label><strong>Horaio de inicio:</strong> @Model.HorarioDeInicio.ToString()</label>
        </div>

        <a href="/sessao/listar" class="btn btn-secondary px-5 mt-4">
            <i class="bi bi-arrow-return-left me-1"></i>
            Voltar
        </a>
    </div>
</div>