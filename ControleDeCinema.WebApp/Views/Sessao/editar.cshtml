@model ControleDeCinema.WebApp.Models.EditarSessaoViewModel


@{
    Layout = "_Layout";
    ViewBag.Title = "Editar Sessao";
}

<div class="row mt-3">
    <div class="col-lg-6">
        <form class="d-grid gap-4" asp-action="Editar">

            <div class="mt-2">
                <label class="control-label fw-semibold">Filme: </label>
                <input class="form-control" asp-for="IdFilme" />
                <span class="text-danger my-1" asp-validation-for="IdFilme"></span>
            </div>

            <div class="mt-2">
                <label class="control-label fw-semibold">Sala: </label>
                <input class="form-control" asp-for="IdSala" id="valor"/>
                <span class="text-danger my-1" asp-validation-for="IdSala"></span>
            </div>

            <div class="mt-2 d-grid gap-2 d-md-block">
                <button class="btn btn-primary px-5">
                    <i class="bi bi-floppy-fill me-1"></i>
                    Gravar
                </button>
                <a href="/sessao/listar" class="btn btn-secondary px-5">
                    <i class="bi bi-arrow-return-left me-1"></i>
                    Voltar
                </a>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function(){
            $("#valor").mask("#.##0,00", { reverse: true });
        });
    </script>
}